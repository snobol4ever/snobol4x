/* Sprint 4: LIT $ OUTPUT — capture literal after skip
 * Pattern: POS(0) LEN(4) LIT("Bird") $ OUTPUT RPOS(0)
 * Subject: "BlueBird"
 * Expected: prints "Bird", returns 0
 */
/* Generated by snobol4x emit_c.py — DO NOT EDIT */
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include "../../src/runtime/runtime.h"

/* === static storage === */
static int64_t cat_l3_saved_cursor;
static str_t var_OUTPUT;
static int64_t cat_l5_start;
static int64_t assign_c7_saved_cursor;

int main(void) {
    const char *subject    = "BlueBird";
    int64_t     subject_len = 8;
    int64_t     cursor      = 0;
    (void)cursor; (void)subject; (void)subject_len;

    goto root_alpha;

    
/* ===== pattern: root ===== */
    root_alpha: /* CAT — enter left */
        goto cat_l1_alpha;
    cat_l1_alpha:
        if (cursor != 0) goto root_beta;
        goto cat_r2_alpha;
    cat_l1_beta:
        goto root_beta;
    cat_r2_alpha: /* CAT — enter left */
        goto cat_l3_alpha;
    cat_l3_alpha:
        if (cursor + 4 > subject_len) goto cat_r2_beta;
        cat_l3_saved_cursor = cursor;
        cursor += 4;
        goto cat_r4_alpha;
    cat_l3_beta:
        cursor = cat_l3_saved_cursor;
        goto cat_r2_beta;
    cat_r4_alpha: /* CAT — enter left */
        goto cat_l5_alpha;
    cat_l5_alpha:
        cat_l5_start = cursor;
        goto assign_c7_alpha;
    assign_c7_alpha:
        if (cursor + 4 > subject_len) goto cat_r4_beta;
        if (memcmp(subject + cursor, "Bird", 4) != 0) goto cat_r4_beta;
        assign_c7_saved_cursor = cursor;
        cursor += 4;
        goto cat_l5_do_assign;
    assign_c7_beta:
        cursor = assign_c7_saved_cursor;
        goto cat_r4_beta;
    cat_l5_do_assign:
        var_OUTPUT.ptr = subject + cat_l5_start;
        var_OUTPUT.len = cursor - cat_l5_start;
        sno_output(var_OUTPUT);
        goto cat_r6_alpha;
    cat_l5_beta:
        goto assign_c7_beta;
    cat_r6_alpha:
        if (cursor != subject_len - 0) goto cat_l5_beta;
        goto root_MATCH_SUCCESS;
    cat_r6_beta:
        goto cat_l5_beta;
    cat_r4_beta:
        goto cat_l3_beta;
    cat_r2_beta:
        goto cat_l1_beta;
    root_beta:
        goto root_MATCH_FAIL;
    root_MATCH_SUCCESS: return 0; /* matched */
    root_MATCH_FAIL:    return 1; /* failed  */
}
